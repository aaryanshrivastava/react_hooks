import logo from "./logo.svg";
import "./App.css";

function App() {
  return (
    <div className="App" style={{ padding: 10 }}>
      <input vlaue={this.this.state.email} onChange={this.handleChange} />
      <p>Email: {this.state.email}</p>
    </div>
  );
}

export default App;
